name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - run: swift build
      - run: .build/debug/RoomMac version
      - run: .build/debug/RoomMac help
